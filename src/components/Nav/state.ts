import { atom } from "jotai";

export const characterSheetUnderlinedAtom = atom<boolean>(false);
export const beefUnderlinedAtom = atom<boolean>(false);
export const battleUnderlinedAtom = atom<boolean>(false);
